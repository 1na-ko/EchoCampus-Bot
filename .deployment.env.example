# ==========================================
# 部署配置文件模板
# 请复制此文件为 .deployment.env 并填入真实信息
# ==========================================

# ==========================================
# 阿里云镜像仓库配置
# ==========================================
ALIYUN_REGISTRY_USERNAME=your-aliyun-username
ALIYUN_REGISTRY_URL=your-registry-url.aliyuncs.com
ALIYUN_REGISTRY_NAMESPACE=your-namespace
ALIYUN_IMAGE_NAME=your-image-name

# 完整镜像地址
FULL_IMAGE_URL=your-registry-url.aliyuncs.com/your-namespace/your-image-name:latest

# ==========================================
# 服务器部署配置
# ==========================================
SERVER_IP=your-server-ip
SERVER_USER=your-username
SERVER_PASSWORD=your-password
SERVER_FRONTEND_PATH=/path/to/frontend/deployment/
SERVER_BACKEND_PORT=8080

# ==========================================
# 快捷命令参考
# ==========================================
# 登录阿里云镜像仓库：
# docker login --username=${ALIYUN_REGISTRY_USERNAME} ${ALIYUN_REGISTRY_URL}

# 标记镜像：
# docker tag ${ALIYUN_IMAGE_NAME}:latest ${FULL_IMAGE_URL}

# 推送镜像：
# docker push ${FULL_IMAGE_URL}

# 前端部署（清空后上传）：
# ssh ${SERVER_USER}@${SERVER_IP} "rm -rf ${SERVER_FRONTEND_PATH}*"
# scp -r ./dist/* ${SERVER_USER}@${SERVER_IP}:${SERVER_FRONTEND_PATH}

# SSH登录服务器：
# ssh ${SERVER_USER}@${SERVER_IP}
