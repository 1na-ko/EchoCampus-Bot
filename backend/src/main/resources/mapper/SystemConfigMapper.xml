<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echocampus.bot.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.echocampus.bot.entity.SystemConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="description" property="description"/>
        <result column="is_editable" property="isEditable"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <select id="selectByKeyPrefix" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE config_key LIKE CONCAT(#{prefix}, '%')
    </select>

    <update id="updateValueByKey">
        UPDATE system_config SET config_value = #{configValue}, updated_at = NOW() WHERE config_key = #{configKey}
    </update>

</mapper>
